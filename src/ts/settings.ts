import {Hsl} from "./Hsl";

export const settings = {
    sky: {
        gradient: ['hsl(200, 95%, 33%)', 'hsl(200, 95%, 80%)']
    },

    nuage: {
        color: 'white',
        radius: 50,
    }
}